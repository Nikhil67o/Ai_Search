apiVersion: v1
kind: Service
metadata:
  name: ai-backend-service
spec:
  type: NodePort # Ya LoadBalancer agar aap AWS/GCP par hain
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
  selector:
    app: ai-backend